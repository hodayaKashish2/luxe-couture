# העתיקי את התוכן לקובץ חדש בשם .env.local בתיקיית הפרויקט

NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

NEXT_PUBLIC_APP_URL=http://localhost:3000
ADMIN_SECRET=בחרי-סיסמה-סודית-ארוכה
ADMIN_EMAIL=dressbclick@gmail.com
# אם ב-Vercel עדיין מוגדר hodayaka1212@gmail.com — הקוד ממפה אוטומטית ל-dressbclick@gmail.com

# === שליחת מיילים ללקוחות (קריטי) ===
# אפשרות 1 (מומלץ): Gmail SMTP — שולח לכל כתובת
# 1. ב-Google Account → אבטחה → הפעילי אימות דו-שלבי
# 2. צרי "סיסמת אפליקציה" (App Password) לדואר
# 3. הדביקי כאן:
SMTP_USER=dressbclick@gmail.com
SMTP_PASSWORD=xxxx-xxxx-xxxx-xxxx
SMTP_FROM_NAME=שמלה בקליק

# אפשרות 2: Resend — רק אחרי אימות דומיין ב-resend.com/domains
# (onboarding@resend.dev שולח רק ל-ADMIN_EMAIL — לא ללקוחות!)
RESEND_API_KEY=your_resend_api_key_here
RESEND_FROM=שמלה בקליק <noreply@YOUR-DOMAIN.com>

# === WhatsApp אוטומטי (אופציונלי) ===
# אפשרות 1 (מומלץ): Green API — green-api.com (חינמי לבדיקות)
# GREEN_API_INSTANCE_ID=your_instance_id
# GREEN_API_TOKEN=your_api_token
#
# אפשרות 2: CallMeBot — דורש הרשמה לכל מספר מקבל
# CALLMEBOT_API_KEY=your_callmebot_key
